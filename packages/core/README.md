# @ycf/core

The deterministic engine behind YCF. It audits and maps repositories, produces supervised plans, and applies only explicitly allowlisted safe cleanup operations.

Version `0.1.0` is released only after the maintainer intentionally runs the npm publication command.

See the main repository for usage, safety boundaries, contribution guidance, and the Apache-2.0 license.
